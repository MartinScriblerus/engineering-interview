import styled from '@emotion/styled';
import { Container, Section, Panel, Typography } from 'ui';

import React from 'react';

export const ProfileSelection = () => {
    return (
        <Container>
            <Section> 
                <Panel>
                    <Typography>
                        Hello From Profile Selection
                    </Typography>
                </Panel>
            </Section>
        </Container>
    )
};
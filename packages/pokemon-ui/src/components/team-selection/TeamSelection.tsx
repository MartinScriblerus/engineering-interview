import styled from '@emotion/styled';
import { Container, Section, Panel, Typography } from 'ui';

import React from 'react';

export const TeamSelection = () => {
    return (
        <Container>
            <Section> 
                <Panel>
                    <Typography>
                        Hello From Team Selection
                    </Typography>
                </Panel>
            </Section>
        </Container>
    )
};
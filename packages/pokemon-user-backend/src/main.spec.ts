import 'reflect-metadata'
import { describe, it, expect } from 'vitest'

describe('pokemon user backend app', async () => {
    it('runs initial test', () => {
        expect(true).toBe(true);
    })
});
